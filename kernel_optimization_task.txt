TASK: Test the kernel optimization tools and optimize a GEMM kernel.

You have access to kernel optimization tools via IPython. Here's how to use them:

## STEP 1: Test the tools work
Run this in IPython to verify the tools are available:
```python
from openhands.runtime.plugins.agent_skills.kernel_tools import (
    evaluate_kernel_quality,
    reflect_on_kernel_result,
    get_optimization_memory,
    add_optimization_attempt,
    should_stop_optimizing,
    get_best_result
)
print("Kernel tools loaded successfully!")
```

## STEP 2: Initialize optimization memory
```python
mem = get_optimization_memory(reset=True, target_speedup=1.2, max_attempts=5)
print(mem)
```

## STEP 3: Read and evaluate a kernel
Read the GEMM kernel from the test file and evaluate it:
- Test file location: /home/upandey/kernelgen/openevolve/TriVolve/aiter/op_tests/test_gemm_a8w8.py
- Look for Triton kernel code in the aiter library

## STEP 4: Run the test to get baseline
```bash
cd /home/upandey/kernelgen/openevolve/TriVolve/aiter/op_tests
python test_gemm_a8w8.py -mnk 1024,4096,1024 2>&1 | head -50
```

## STEP 5: Record the attempt and reflect
Use `add_optimization_attempt()` and `reflect_on_kernel_result()` to analyze.

## STEP 6: Check if should continue
Use `should_stop_optimizing()` and `get_best_result()`.

GOAL: Verify the kernel optimization tools work correctly. 
You don't need to actually optimize anything - just test that the tools function properly.
