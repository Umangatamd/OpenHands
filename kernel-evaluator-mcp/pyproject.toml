[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kernel-evaluator-mcp"
version = "0.1.0"
description = "MCP server for LLM-based Triton/AMD GPU kernel evaluation"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    {name = "AMD GEAK Team"}
]
keywords = ["mcp", "triton", "gpu", "kernel", "amd", "rocm", "llm", "evaluation"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "mcp>=1.0.0",
    "fastmcp>=2.0.0",
    "anthropic>=0.30.0",
    "litellm>=1.40.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
]

[project.scripts]
kernel-evaluator-mcp = "kernel_evaluator_mcp.server:main"

[project.urls]
Homepage = "https://github.com/amd/kernel-evaluator-mcp"
Documentation = "https://github.com/amd/kernel-evaluator-mcp#readme"
Repository = "https://github.com/amd/kernel-evaluator-mcp"

[tool.hatch.build.targets.wheel]
packages = ["src/kernel_evaluator_mcp"]
